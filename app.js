// app.js
const App = () => {
    return (
        <div>
            <h1>Hello, ReactJS!</h1>
            <p>This is a simple ReactJS component rendered in HTML.</p>
        </div>
    );
};

ReactDOM.render(<App />, document.getElementById('root'));
